"use strict";

/*var UI = {

	affiche_liste_events : function(data, callback){
		for (var i=0;i<data.events.length;i++){
			
			
			//   APPELER LES DONNEES RECUPEREES DU FICHIER JSON : 
			//   data.events[i].champ_du_fichier_JSON
			
			console.log(data.events[i].titre);
			console.log(data.events[i].sous_titre);
			console.log(data.events[i].image);
			console.log(data.events[i].timestamp);
			console.log(data.events[i].lat);
			console.log(data.events[i].lng);
			console.log('–––––––––––––––––––––––––––––––––');			
			
			
	//			=> utilisable également depuis control.js en callback de model.getJSON
		}
		callback.call(this);									// signifie à control.js que c'est fini
	}
}*/